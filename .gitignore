# tooling / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.claude/

# generated artifacts (regenerate; not source)
*.png
out/
renders/
report.html

# ...but DO commit the curated showcase assets (hero renders + a copy of the report).
# (comments must be on their own line in .gitignore; an inline # is part of the pattern)
!docs/
!docs/**
